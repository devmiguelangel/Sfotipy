<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="match_parent">

    <ImageView
        android:src="@drawable/header_bg"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:scaleType="centerCrop"
        android:id="@+id/avatar_song"
        android:layout_marginRight="15dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceLarge"
        android:text="Large Text"
        android:id="@+id/nombre_song"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@+id/avatar_song" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:attr/textAppearanceSmall"
        android:text="Small Text"
        android:id="@+id/artist_song"
        android:layout_toEndOf="@+id/avatar_song"
        android:layout_below="@+id/nombre_song"
        android:layout_marginTop="10dp" />

    <RatingBar
        android:numStars="4"
        android:isIndicator="true"
        android:max="4"
        android:stepSize="0.01"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/stars_song"
        android:layout_below="@+id/artist_song"
        android:layout_toEndOf="@+id/avatar_song"
        android:layout_marginTop="10dp" />

</RelativeLayout>
